-- Migration: 20260920120000_claims_claimant_delete_and_status_check
-- Purpose: Allows claimant to delete pending/withdrawn claims and adds withdrawn status (already applied on remote database).
SELECT 1;
